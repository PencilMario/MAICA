# Intent

Merge `upstream/main`, retain disabled GitHub Actions behavior, and implement unconditional fixed shared-account authentication when `SERVICE_ONLY=1`.

Non-goals: token-derived accounts, remote-safe service-only security, preserving deleted authentication owners.

